//Define una función que reciba un número y devuelva su cuadrado.
//  Escríbela de tres maneras:
function cuadrado1(num) {
    return num * num;
}

const cuadrado2 = function(num) {
    return num * num;
};

const cuadrado3 = (num) => {
    return num * num;
};